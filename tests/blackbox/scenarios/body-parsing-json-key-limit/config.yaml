version: v1alpha1
port: 18080

global:
  detect_only: false

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - application/json
    inspection:
      json_keys: 5
