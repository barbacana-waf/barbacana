version: v1alpha1
listen: ":18080"

global:
  detect_only: false

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - application/json
        - application/xml
    inspection:
      json_depth: 5
      xml_depth: 5
      xml_entities: 10
