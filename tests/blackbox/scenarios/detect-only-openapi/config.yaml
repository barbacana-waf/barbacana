version: v1alpha1
port: 18080

global:
  mode: detect

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - application/json
    openapi:
      spec: tests/blackbox/scenarios/detect-only-openapi/spec.yaml
      strict: true
