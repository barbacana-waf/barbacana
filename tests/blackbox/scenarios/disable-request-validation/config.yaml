version: v1alpha1
port: 18080

global:
  detect_only: false

routes:
  - upstream: http://localhost:19000
    accept:
      max_body_size: 100
      methods: [GET, POST, DELETE]
    disable:
      - max-body-size
