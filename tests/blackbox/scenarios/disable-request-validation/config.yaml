version: v1alpha1
port: 18080

global:
  mode: blocking

routes:
  - upstream: http://localhost:19000
    accept:
      max_body_size: 100
      methods: [GET, POST, DELETE]
    disable:
      - max-body-size
