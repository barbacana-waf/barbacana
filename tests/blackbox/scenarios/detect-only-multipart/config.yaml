version: v1alpha1
port: 18080

global:
  detect_only: true

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - multipart/form-data
    multipart:
      double_extension: true
