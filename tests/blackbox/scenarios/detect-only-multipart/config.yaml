version: v1alpha1
port: 18080

global:
  mode: detect

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - multipart/form-data
    multipart:
      double_extension: true
