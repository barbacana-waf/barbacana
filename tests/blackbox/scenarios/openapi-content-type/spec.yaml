openapi: "3.0.3"
info:
  title: Test API
  version: "1.0"
paths:
  /items:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
              required: [name]
      responses:
        "200":
          description: OK
    get:
      responses:
        "200":
          description: OK
