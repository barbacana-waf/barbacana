version: v1alpha1
port: 18080

global:
  mode: blocking

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - application/json
    openapi:
      spec: tests/blackbox/scenarios/openapi-invalid-params/spec.yaml
      strict: true
