version: v1alpha1
listen: ":18080"

global:
  detect_only: false

routes:
  - upstream: http://localhost:19000
    accept:
      content_types:
        - application/xml
    inspection:
      xml_depth: 3
